login credentials are {username : sreeman ; password : 1234!}
and for the otp part to work u need to add mail id (from which otp is sent) 
and the mail's password key(not the password of the mail, its like an api key)
search smtplib in main.py and add mail id and key for otp to work.
Without otp : just login using credentials,but u cant create account or reset password.